/*
#pragma once
#include <deprecated.h>
#include <MFRC522.h>
#include <MFRC522Extended.h>
#include <require_cpp11.h>

class RFScanner {
public:
    RFScanner(); // Constructor
    void init(); // Initialize the RFID scanner
    void scan(); // Scan for RFID tags

private:
    MFRC522 mfrc522; // Create MFRC522 instance.
};
*/
